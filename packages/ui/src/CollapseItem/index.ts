import { withNoopInstall } from '../helpers/with-install'
import { CollapseItem } from '../Collapse'

export { CollapseItem }
export default CollapseItem
export const install = withNoopInstall(CollapseItem)
