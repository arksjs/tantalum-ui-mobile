import { withInstall } from '../helpers/with-install'
import { PickerPopup, showPicker } from '../Picker'

export { PickerPopup, showPicker }
export default PickerPopup
export const install = withInstall(PickerPopup)
