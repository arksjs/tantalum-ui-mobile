import { withNoopInstall } from '../helpers/with-install'
import { TabViewItem } from '../TabView'

export { TabViewItem }
export default TabViewItem
export const install = withNoopInstall(TabViewItem)
