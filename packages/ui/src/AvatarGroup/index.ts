import { withNoopInstall } from '../helpers/with-install'
import { AvatarGroup } from '../Avatar'

export { AvatarGroup }
export default AvatarGroup
export const install = withNoopInstall(AvatarGroup)
