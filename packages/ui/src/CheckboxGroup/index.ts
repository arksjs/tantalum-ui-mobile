import { withNoopInstall } from '../helpers/with-install'
import { CheckboxGroup } from '../Checkbox'

export { CheckboxGroup }
export default CheckboxGroup
export const install = withNoopInstall(CheckboxGroup)
