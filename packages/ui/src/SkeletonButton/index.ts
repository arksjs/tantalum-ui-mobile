import { withNoopInstall } from '../helpers/with-install'
import { SkeletonButton } from '../Skeleton'

export { SkeletonButton }
export default SkeletonButton
export const install = withNoopInstall(SkeletonButton)
