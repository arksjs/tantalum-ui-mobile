import { withNoopInstall } from '../helpers/with-install'
import { ButtonGroup } from '../Button'

export { ButtonGroup }
export default ButtonGroup
export const install = withNoopInstall(ButtonGroup)
